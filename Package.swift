// swift-tools-version:5.3

import PackageDescription

let package = Package(
    name: "swiftui-apnsManager",
    products: [
        .library(
            name: "swiftui-apnsManager",
            targets: ["swiftui-apnsManager"]),
    ],
    dependencies: [
    ],
    targets: [
        .target(
            name: "swiftui-apnsManager",
            dependencies: []),
    ]
)
